# 끄숑 · 개선 과제 (있어야 하지만 지금 어설픈 것)

> 작성일: 2026-06-15
> 대상: 팀 공유용. "기능을 새로 더하는" 문서가 아니라, **이미 있는데 완성도가 부족한 부분**을 모아 우선순위를 함께 정하기 위한 문서.
> 코드 기준: `app.js`, `index.html` (프로토타입)

---

## ⭐ 먼저 합의해야 할 핵심 결정

**캐릭터 상태(5단계)를 무엇 기준으로 판정할 것인가?**

- 현재: `levelOf()`가 **절대 사용시간(시)** 기준 (0~2h=1단계 … 7h+=5단계) — `app.js:134`
- 문제: 목표시간은 사용자가 1~5시간으로 바꿀 수 있는데, 캐릭터 단계는 목표와 무관하게 고정.
  - 예) 목표를 2시간으로 잡은 사람이 3시간 사용 → "목표 초과(경고)"인데 캐릭터는 여전히 **1단계(아주 좋은 흐름)**.
  - 결과: **목표 대비 상태**와 **캐릭터 코칭 톤**이 따로 놀아 코칭 일관성이 깨짐.
- 선택지:
  - (A) 절대 사용시간 기준 유지 (목표는 별도 보조 지표) — 단순함
  - (B) 목표 대비 비율 기준으로 단계 판정 — 개인화·일관성↑
  - (C) 혼합 (절대시간 + 목표초과 시 가중)
- **→ 이 결정이 아래 과제 대부분의 방향을 좌우하므로 가장 먼저 정한다.**

---

## 1. 온보딩 / 권한 흐름이 1회성

- **현상**: `onboarded=true`가 한 번 저장되면 다시 진입 불가. 권한 거부·재동의 흐름이 없음. (`app.js:292`, `367`)
- **왜 문제**: 실제 앱의 핵심은 Android Usage Stats 권한인데, 거부했을 때/나중에 켤 때의 경로가 없음.
- **개선 방향**:
  - 권한 미동의/거부 상태를 구분해 안내 화면 유지
  - 설정성 재진입 경로(또는 권한 안내 배너)
  - 프로토타입에서는 "온보딩 다시 보기" 디버그 진입점이라도 확보

## 2. 목표 초과와 캐릭터 단계가 분리됨

- **현상**: 위 핵심 결정과 직결. `levelOf()`는 절대시간, 목표대비 상태(`goalStatus`)는 따로 계산. (`app.js:134`, `220`)
- **왜 문제**: "목표 넘었어요" 경고가 떠도 캐릭터는 웃고 있는 모순.
- **개선 방향**: 핵심 결정(A/B/C) 확정 후, 캐릭터 단계·말풍선 톤·목표 상태가 하나의 기준으로 움직이게 통일.

## 3. 기록 화면 "가장 적은 날" 강조

- **현상**: 하단 통계가 *일/월 평균* + *가장 적게 쓴 날/달*. (`app.js:255`)
- **왜 문제**: 디지털디톡스 앱의 동기부여 관점에선 "가장 적게 쓴 날"보다 **목표 달성 일수**나 **가장 많이 쓴 날(경각심)**이 더 맞을 수 있음.
- **개선 방향**:
  - "이번 주 목표 달성 N일" 류의 성취 지표 검토
  - 또는 가장 많은 날(주의 환기)과 적은 날(칭찬) 중 컨셉에 맞게 선택

## 4. 사용시간이 고정 임시값 (데모 시연성)

- **현상**: `usage` 기본값 135분 하드코딩, 화면에서 바꿀 방법 없음. (`app.js:5`)
- **왜 문제**: 단계별(1~5단계) 캐릭터·멘트 변화가 이 앱의 핵심인데, 데모에서 그 변화를 보여줄 수 없음.
- **개선 방향**: 데모용 슬라이더/버튼으로 사용시간을 조절해 단계 전환을 시연 가능하게. (출시 빌드에선 숨김)

## 5. 멘트가 날짜 시드로 하루 고정

- **현상**: `daySeed()`로 같은 날은 같은 멘트, 클릭해도 안 바뀜. (`app.js:163`, `203`)
- **왜 문제**: 의도된 설계지만("그날 하루는 고정"), 기획서의 "실제로 말 거는 느낌"과는 다소 거리. 재방문 시 신선함 약함.
- **개선 방향**: 하루 고정 유지 vs 방문/시간대마다 순환 — 컨셉 측면에서 한 번 재검토.

---

## 참고: 함께 검토하면 좋은 것 (기획엔 있으나 아직 없음)

- **목표 달성/실패 마무리 피드백** (도장·뱃지 등) — 디톡스 동기의 핵심
- **월간 막대 그래프에 목표선 표시** — 목표 대비 흐름을 그래프에서 바로 보기

---

## 논의 결과 기록 (회의 후 채우기)

| 과제 | 결정 | 담당 | 비고 |
|---|---|---|---|
| ⭐ 단계 판정 기준 (A/B/C) | | | |
| 1. 온보딩/권한 흐름 | | | |
| 2. 목표-캐릭터 통일 | | | |
| 3. 기록 통계 지표 | | | |
| 4. 데모 시연 조절 | | | |
| 5. 멘트 노출 방식 | | | |
