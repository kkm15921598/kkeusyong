# 끄숑 · 작업내역 (Worklog)

> 커밋 & 푸시할 때마다 이 파일에 작업 내용을 정리한다. (최신 항목이 위로)
> 형식: `날짜 · 커밋해시 — 한 줄 요약` + 세부 항목

---

## 2026-06-18 · 실제 휴대폰에서 화면 꽉 채우기 + 캐릭터 위치 조정

- **모바일 풀스크린**: 좁은 화면(≤480px·실제 폰)에서 `390×844` 목업 프레임을 벗고 `.device`를 `100dvw×100dvh`로 꽉 채움. `border-radius`·테두리·그림자 제거(둥근 박스/여백 사라짐). `transform` 스케일도 끔. 데스크톱 미리보기는 그대로 유지.
- 구현: `styles.css`에 `@media (max-width:480px)` 추가, `app.js`의 `fitDevice()`에 폰 가드(스케일 미적용).
- **캐릭터 위치**: 홈 끄숑 캐릭터를 위로 30px 이동(`.character` 하단 마진 `-12px → 18px`).
- **하단 카드 잘림 해결(모바일)**: 캐릭터 이미지 `272px`·말풍선 위 `61px` 고정값 때문에 짧은 폰(갤럭시 S23 등)에서 하단 카드가 잘리던 문제 수정. 모바일 미디어쿼리에서 `.character`/`.char-stage`를 `flex:1 1 auto; min-height:0`로 유동화, `.char-img`를 `height:100%; max-height:272px`로 자동 축소(공간 부족 시 줄고, 클 땐 272px 유지). 말풍선 위 여백 `61px→8px`, `overflow-y:auto` 안전장치 추가 → 모든 폰에서 한 화면에 표시.

## 2026-06-15 · `36182fb` — 온보딩 3장·홈 카드 통합·폰트 교체·기록 통계 개편

- **온보딩 3장 캐러셀** 신규: ① 환영 → ② 작동 방식(칭찬/걱정/T·F) → ③ 권한 동의. 진행 점·다음·건너뛰기·좌우 스와이프 지원.
- **홈 하단 카드 통합**: '오늘의 상태' + '오늘 사용시간' 두 박스를 하나의 글래스 카드로 합침. 구성: 표정+이름+컨디션% → 목표 대비 문구·게이지 → 구분선 → **오늘/목표/남은(초과) 3분할**. (일러스트 슬롯 deco 제거)
- **글씨 가독성**: 단계별 진한 글씨색 `--accent-ink` 도입(색상 코딩 유지), 전체 글씨 크기 +1단계.
- **폰트 교체**: Pretendard → **페이퍼로지(Paperlogy)** (SIL OFL · 상업적 이용 가능, 굵기 100~900).
- **말풍선**: 한국어 단어(어절) 단위 줄바꿈(`word-break:keep-all`) + 위치 상단 조정.
- **기록 통계**: '가장 적은 날/달' → '가장 많은 날/달'(경각심).
- `docs/개선과제.md` 갱신(1·3번 진척, 6번 실데이터 연동 추가, 변경 이력).

## 2026-06-15 · `3a5bf2a` — 캐릭터 상태를 목표 대비 비율 기준으로 통일

- 캐릭터 5단계 판정을 **절대 사용시간 → 목표 대비 비율 `r = 사용시간/목표`** 로 변경.
- `r=1.0`(목표 도달)에서 '목표 초과' 경고와 캐릭터 경고 단계가 동시 전환 → 목표 상태와 캐릭터가 따로 놀지 않음.
- 구현: `STATE_BANDS` / `ratioOf()` / `levelOf(min,goal)` / `energyOf(min,goal)`. 컨디션 게이지도 같은 구간에 묶음.
- `docs/기획안.md` 10장·`docs/개선과제.md` 새 기준으로 갱신.

## 2026-06-15 · `033989f` — 개선 과제 정리 문서 추가 (팀 공유용)

- `docs/개선과제.md` 신규: "있어야 하지만 어설픈 것" 정리(온보딩/목표-캐릭터/기록 지표/데모 시연/멘트 노출 등) + 회의용 결과표.

---

## 이전 기록 (작업내역 파일 도입 전, git log 기준)

- **2026-06-10 · `86c1cc2`** — 홈/기록 글래스모피즘 리디자인 · 일러스트 슬롯 · 4단계 시간대 배경
- **2026-06-10 · `3c505ef`** — 상단 헤더(휴대폰 끄숑) 제거 및 토글 상단 여백 조정
- **2026-06-10 · `299b56e`** — 홈/기록 UX 개편 · 시간대별 배경 · Pretendard 폰트
- **2026-06-10 · `c8e7ccf`** — 홈 화면 끄슝 이미지 기반 리디자인 + 5단계 상태 트래커
- **2026-06-10 · `e51dde9`** — 캐릭터 이미지/시안 업로드용 assets 폴더 추가
- **2026-06-10 · `d627138`** — 디지털디톡스 코치 앱 기획안 및 프로토타입 추가
